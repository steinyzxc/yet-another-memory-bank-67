---
description: Save a durable fact to mcb persistent memory.
---

Save this durable memory to mcb: `$ARGUMENTS`

Use `mcp__mcb__memory_save` with the current project when known. Save only stable preferences, decisions, architecture facts, workflows, bugs, or lessons that should survive future sessions. Do not save secrets, temporary command output, speculation, or raw session IDs.

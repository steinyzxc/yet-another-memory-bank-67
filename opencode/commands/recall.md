---
description: Search mcb persistent memory for relevant context.
---

Search mcb memory for: `$ARGUMENTS`

Use `mcp__mcb__memory_search` first with the provided query and the current project when known. If results are too sparse, retry with `mcp__mcb__memory_recall` so useful memories get their access metadata refreshed.

Return the most relevant facts with concise citations to memory IDs when available.
